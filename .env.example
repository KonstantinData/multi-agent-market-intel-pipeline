# OpenAI API key (canonical key name used by agents)
OPENAI_API_KEY=

# Required pipeline version when --pipeline-version is not provided
PIPELINE_VERSION=

# Optional metadata for run snapshots
GIT_SHA=
