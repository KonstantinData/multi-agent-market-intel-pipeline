# Testing
pytest>=8.2,<9.0
pytest-cov>=5.0,<6.0

# Linting / formatting (fast + strict)
ruff>=0.5,<1.0

# Type checking
mypy>=1.10,<2.0
types-PyYAML>=6.0,<7.0
types-python-dateutil>=2.9,<3.0

# Validation / schemas (developer utilities)
jsonschema>=4.23,<5.0

# Security baseline checks (optional but recommended)
bandit>=1.7,<2.0

# Developer ergonomics
pre-commit>=3.7,<4.0
